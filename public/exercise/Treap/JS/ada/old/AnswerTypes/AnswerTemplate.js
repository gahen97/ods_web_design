/*jshint esversion: 6 */ 'use strict';

class AnswerTemplate extends AnswerType {
  constructor()
  {
    super();

    // anything else needed
  }

  check (userAnswer)
  {
    // some way to check if this matches user answer.
    // note, userAnswer is a model
  }
}
