/*
  I don't know what to call this so I'm calling it DivMath.
*/

class DivMath {
  static computeSlope (from, to) {
    var p1 = Div.dimensionsOf (from);
    var p2 = Div.dimensionsOf (to);

    console.log (p1, p2);
  }
}
